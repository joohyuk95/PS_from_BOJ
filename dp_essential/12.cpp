#include <iostream>
#include <algorithm>
using namespace std;

int n, k;
int value[101];
int dp[10001];

int main() 
{
    ios_base::sync_with_stdio(false);
    cin >> n >> k;

    for (int i = 1; i <= n; ++i) {
        cin >> value[i];
    }
    sort(value, value + n);

    for (int i = 1; i <= n; ++i) {
        for (int j = 1; j <= k; ++j) {
            ;
        }
    }

    if (true) {
        ;
    } else {
        ;
    }

    return 0;
}